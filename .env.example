# Anthropic API Key for Claude
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Exa API Key for web search
# Get your key at: https://exa.ai/
EXA_API_KEY=your_exa_api_key_here

# =============================================================================
# Vercel Sandbox (Required for Production Deployment)
# =============================================================================
# The Vercel Sandbox provides isolated containers for agent execution.
# This is necessary because Vercel serverless functions cannot spawn
# subprocesses, which the Claude Agent SDK requires.
#
# These variables are ONLY needed for production deployment on Vercel.
# Local development works without them (uses direct execution).
# =============================================================================

# Vercel API Token
# Create at: https://vercel.com/account/tokens
VERCEL_TOKEN=your_vercel_api_token_here

# Vercel Project ID
# Found in: Project Settings → General → Project ID
VERCEL_PROJECT_ID=your_vercel_project_id_here

# Vercel Team ID (Optional - only needed for team projects)
# Found in: Team Settings → General → Team ID
# VERCEL_TEAM_ID=your_vercel_team_id_here
